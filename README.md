# example3

Quick update:

I've now completed:

The generation of all ARDs in CSV format.
The merged ARDs for studies that had multiple ADaM folders available in Domino.

The only remaining task is uploading the files to Thea.View. The final structure for each study will be:

Original ARD (.xlsx) for reference.
ARD (.csv) for Arges/data consumption.
PARAMCD dictionary (.csv) containing the variable metadata.

This is the structure that will be available in Thea.View once the upload is completed.
