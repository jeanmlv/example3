# example3

Thanks, @Gunderson, Fabio, I saw that you used the merged ARD file that I built. Since the ARD is generated by integrating all available ADaM datasets into a single patient-level structure (USUBJID + AVISIT), it's expected to have a large number of columns, as it includes variables from around 25 ADaM domains.

From my perspective, I don't think we necessarily need all ~3K columns for Arges. It would probably be better to include only the variables relevant to the Arges use cases (e.g., ADMAYO_RBSCORE, ADMAYO_PGSCORE, etc.), which would keep the dataset smaller and easier to work with.

We can align with Pablo on the metadata/variables that are actually needed.
